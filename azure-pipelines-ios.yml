trigger:
  - main

pool:
  name: 'Default'
  demands:
    - Agent.Name -equals dogcatify-mac

variables:
  - group: ios-signing

  - name: NODE_VERSION
    value: '18.x'

  - name: IOS_SCHEME
    value: 'DogCatiFy'

  - name: IOS_CONFIGURATION
    value: 'Release'

  - name: KEYCHAIN_NAME
    value: 'build.keychain'

steps:
  - checkout: self
    clean: true
    fetchDepth: 0

  - task: NodeTool@0
    displayName: 'Instalar Node.js'
    inputs:
      versionSpec: '$(NODE_VERSION)'

  - script: |
      set -e
      node -v
      npm -v
    displayName: 'Ver versiones Node/NPM'

  - script: |
      set -e
      npm ci
    displayName: 'Instalar dependencias (npm ci)'

  # Si no existe ios/, lo genera (nativo). Si existe, NO lo regenera.
  - script: |
      set -e
      if [ ! -d "ios" ]; then
        echo "No existe carpeta ios/, ejecutando prebuild..."
        npx expo prebuild -p ios --no-install
      else
        echo "Existe carpeta ios/, saltando expo prebuild."
      fi
    displayName: 'Expo prebuild iOS (si aplica)'

  - script: |
      set -e
      cd ios
      pod install
    displayName: 'Instalar CocoaPods'

  # ---- Secure files ----
  - task: DownloadSecureFile@1
    name: iosCert
    displayName: 'Descargar .p12'
    inputs:
      secureFile: 'dogcatify-ios.p12'

  - task: DownloadSecureFile@1
    name: iosProfile
    displayName: 'Descargar provisioning profile'
    inputs:
      secureFile: 'DogCatiFy_AppStore.mobileprovision'

  # ---- Keychain + importar .p12 + instalar profile ----
  - script: |
      set -e

      CERT_PATH="$(iosCert.secureFilePath)"
      PROFILE_PATH="$(iosProfile.secureFilePath)"

      echo "CERT_PATH=$CERT_PATH"
      echo "PROFILE_PATH=$PROFILE_PATH"

      security delete-keychain "$(KEYCHAIN_NAME)" || true

      security create-keychain -p "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"
      security set-keychain-settings -t 3600 -l "$(KEYCHAIN_NAME)"
      security unlock-keychain -p "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"

      security list-keychains -d user -s "$(KEYCHAIN_NAME)" login.keychain-db
      security default-keychain -s "$(KEYCHAIN_NAME)"

      security import "$CERT_PATH" -k "$(KEYCHAIN_NAME)" -P "$(IOS_P12_PASSWORD)" -A -T /usr/bin/codesign -T /usr/bin/security
      security set-key-partition-list -S apple-tool:,apple: -s -k "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"

      UUID=$(/usr/bin/security cms -D -i "$PROFILE_PATH" | /usr/bin/plutil -extract UUID raw -o - -)
      echo "Provisioning UUID: $UUID"

      mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
      cp "$PROFILE_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/$UUID.mobileprovision"

      echo "OK: keychain + p12 + provisioning instalados"
    displayName: 'Keychain + importar .p12 + instalar .mobileprovision'
    env:
      IOS_P12_PASSWORD: $(IOS_P12_PASSWORD)
      MAC_KEYCHAIN_PASSWORD: $(MAC_KEYCHAIN_PASSWORD)

  # ---- Detectar workspace real ----
  - script: |
      set -e
      WS=$(find ios -maxdepth 3 -name "*.xcworkspace" -print | head -n 1)
      if [ -z "$WS" ]; then
        echo "ERROR: No encontré .xcworkspace dentro de /ios"
        exit 1
      fi

      echo "Workspace encontrado: $WS"
      echo "##vso[task.setvariable variable=IOS_WORKSPACE]$WS"

      echo "Listando schemes:"
      xcodebuild -list -workspace "$WS"
    displayName: 'Detectar workspace + listar schemes'

  # ---- Crear exportOptions.plist en STAGING (ruta segura) ----
  - script: |
      set -e

      EXPORT_PLIST="$(Build.ArtifactStagingDirectory)/exportOptions.plist"

      cat > "$EXPORT_PLIST" <<EOF
      <?xml version="1.0" encoding="UTF-8"?>
      <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      <plist version="1.0">
      <dict>
        <key>method</key>
        <string>$(IOS_EXPORT_METHOD)</string>

        <key>teamID</key>
        <string>$(IOS_TEAM_ID)</string>

        <key>signingStyle</key>
        <string>manual</string>

        <key>provisioningProfiles</key>
        <dict>
          <key>$(IOS_BUNDLE_ID)</key>
          <string>DogCatiFy_AppStore</string>
        </dict>
      </dict>
      </plist>
      EOF

      echo "Export plist creado en: $EXPORT_PLIST"
      ls -la "$(Build.ArtifactStagingDirectory)"
      plutil -p "$EXPORT_PLIST" || cat "$EXPORT_PLIST"

      echo "##vso[task.setvariable variable=EXPORT_PLIST]$EXPORT_PLIST"
    displayName: 'Crear exportOptions.plist (staging)'

  # =========================
  # ARCHIVE iOS (NATIVO)
  # =========================
  - script: |
      set -euo pipefail

      ARCHIVE_PATH="$(Build.ArtifactStagingDirectory)/DogCatiFy.xcarchive"

      echo "Archiving usando xcodeproj (Expo-friendly)"
      xcodebuild \
        -project ios/DogCatiFy.xcodeproj \
        -scheme DogCatiFy \
        -configuration Release \
        -sdk iphoneos \
        -destination "generic/platform=iOS" \
        -archivePath "$ARCHIVE_PATH" \
        DEVELOPMENT_TEAM="$(IOS_TEAM_ID)" \
        PRODUCT_BUNDLE_IDENTIFIER="$(IOS_BUNDLE_ID)" \
        CODE_SIGN_STYLE=Manual \
        PROVISIONING_PROFILE_SPECIFIER="DogCatiFy_AppStore" \
        clean archive

      echo "Archive generado en:"
      ls -la "$ARCHIVE_PATH"
    displayName: 'Archive iOS (NATIVO - xcodeproj)'


  # =========================
  # EXPORT IPA
  # =========================
  - script: |
      set -euo pipefail

      EXPORT_PATH="$(Build.ArtifactStagingDirectory)/export"
      EXPORT_PLIST="$(Build.ArtifactStagingDirectory)/exportOptions.plist"

      cat > "$EXPORT_PLIST" <<EOF
      <?xml version="1.0" encoding="UTF-8"?>
      <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      <plist version="1.0">
      <dict>
        <key>method</key>
        <string>app-store</string>

        <key>teamID</key>
        <string>$(IOS_TEAM_ID)</string>

        <key>signingStyle</key>
        <string>manual</string>

        <key>provisioningProfiles</key>
        <dict>
          <key>$(IOS_BUNDLE_ID)</key>
          <string>DogCatiFy_AppStore</string>
        </dict>
      </dict>
      </plist>
      EOF

      xcodebuild \
        -exportArchive \
        -archivePath "$(Build.ArtifactStagingDirectory)/DogCatiFy.xcarchive" \
        -exportPath "$EXPORT_PATH" \
        -exportOptionsPlist "$EXPORT_PLIST"

      echo "IPA generado:"
      find "$EXPORT_PATH" -name "*.ipa" -print
    displayName: 'Export IPA'


  - task: PublishBuildArtifacts@1
    displayName: 'Publicar artefacto iOS'
    inputs:
      pathToPublish: '$(Build.ArtifactStagingDirectory)/export'
      artifactName: 'ios-ipa'
      publishLocation: 'Container'

  - task: PublishBuildArtifacts@1
    displayName: 'Publicar logs'
    condition: succeededOrFailed()
    inputs:
      pathToPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'ios-build-logs'
      publishLocation: 'Container'
