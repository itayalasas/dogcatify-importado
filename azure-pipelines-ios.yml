trigger:
  - main

pool:
  name: 'Default'
  demands:
    - Agent.Name -equals dogcatify-mac

variables:
  - group: ios-signing

  - name: NODE_VERSION
    value: '18.x'

  - name: IOS_SCHEME
    value: 'DogCatiFy'

  - name: IOS_CONFIGURATION
    value: 'Release'

  - name: KEYCHAIN_NAME
    value: 'build.keychain'

steps:
  - checkout: self
    clean: true
    fetchDepth: 0

  - task: NodeTool@0
    displayName: 'Instalar Node.js'
    inputs:
      versionSpec: '$(NODE_VERSION)'

  - script: |
      set -e
      node -v
      npm -v
    displayName: 'Ver versiones Node/NPM'

  - script: |
      set -e
      npm ci
    displayName: 'Instalar dependencias (npm ci)'

  # Genera ios/ nativo si no existe
  - script: |
      set -e
      if [ ! -d "ios" ]; then
        echo "No existe ios/, ejecutando expo prebuild..."
        npx expo prebuild -p ios --no-install
      else
        echo "Existe ios/, saltando expo prebuild."
      fi
    displayName: 'Expo prebuild iOS (si aplica)'

  - script: |
      set -e
      cd ios
      pod install
    displayName: 'Instalar CocoaPods'

  # ---- Secure files ----
  - task: DownloadSecureFile@1
    name: iosDistCert
    displayName: 'Descargar .p12 (Apple Distribution)'
    inputs:
      secureFile: 'dogcatify-dist.p12'

  # (Opcional) si querés importar también Apple Development
  # - task: DownloadSecureFile@1
  #   name: iosDevCert
  #   displayName: 'Descargar .p12 (Apple Development)'
  #   inputs:
  #     secureFile: 'dogcatify-dev.p12'

  - task: DownloadSecureFile@1
    name: iosProfile
    displayName: 'Descargar provisioning profile (AppStore)'
    inputs:
      secureFile: 'DogCatiFy_AppStore.mobileprovision'

  # ---- Keychain + importar certs + instalar provisioning ----
  - script: |
      set -e

      CERT_DIST_PATH="$(iosDistCert.secureFilePath)"
      PROFILE_PATH="$(iosProfile.secureFilePath)"

      echo "CERT_DIST_PATH=$CERT_DIST_PATH"
      echo "PROFILE_PATH=$PROFILE_PATH"

      security delete-keychain "$(KEYCHAIN_NAME)" || true

      security create-keychain -p "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"
      security set-keychain-settings -t 3600 -l "$(KEYCHAIN_NAME)"
      security unlock-keychain -p "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"

      security list-keychains -d user -s "$(KEYCHAIN_NAME)" login.keychain-db
      security default-keychain -s "$(KEYCHAIN_NAME)"

      echo "Importando Apple Distribution..."
      security import "$CERT_DIST_PATH" -k "$(KEYCHAIN_NAME)" -P "$(IOS_P12_PASSWORD)" -A -T /usr/bin/codesign -T /usr/bin/security
      security set-key-partition-list -S apple-tool:,apple: -s -k "$(MAC_KEYCHAIN_PASSWORD)" "$(KEYCHAIN_NAME)"

      # (Opcional) Importar Apple Development si lo necesitás
      # CERT_DEV_PATH="$(iosDevCert.secureFilePath)"
      # echo "Importando Apple Development..."
      # security import "$CERT_DEV_PATH" -k "$(KEYCHAIN_NAME)" -P "$(IOS_DEV_P12_PASSWORD)" -A -T /usr/bin/codesign -T /usr/bin/security

      echo "Identidades disponibles:"
      security find-identity -v -p codesigning "$(KEYCHAIN_NAME)" || true

      UUID=$(/usr/bin/security cms -D -i "$PROFILE_PATH" | /usr/bin/plutil -extract UUID raw -o - -)
      NAME=$(/usr/bin/security cms -D -i "$PROFILE_PATH" | /usr/bin/plutil -extract Name raw -o - -)
      echo "Provisioning Name: $NAME"
      echo "Provisioning UUID: $UUID"

      mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
      cp "$PROFILE_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/$UUID.mobileprovision"

      echo "Chequear entitlements del profile (Associated Domains):"
      /usr/bin/security cms -D -i "$PROFILE_PATH" > /tmp/profile.plist
      /usr/bin/plutil -extract Entitlements xml1 -o - /tmp/profile.plist | head -n 120 || true
    displayName: 'Keychain + importar certs + instalar provisioning'
    env:
      IOS_P12_PASSWORD: $(IOS_P12_PASSWORD)
      IOS_DEV_P12_PASSWORD: $(IOS_DEV_P12_PASSWORD)
      MAC_KEYCHAIN_PASSWORD: $(MAC_KEYCHAIN_PASSWORD)

  # ---- Detectar workspace/scheme (nativo) ----
  - script: |
      set -e
      WS=$(find ios -maxdepth 3 -name "*.xcworkspace" -print | head -n 1)
      if [ -z "$WS" ]; then
        echo "ERROR: No encontré .xcworkspace dentro de ios/"
        exit 1
      fi
      echo "Workspace: $WS"
      echo "##vso[task.setvariable variable=IOS_WORKSPACE]$WS"

      echo "Schemes:"
      xcodebuild -list -workspace "$WS"
    displayName: 'Detectar workspace + listar schemes'

  # ---- Crear exportOptions.plist (ruta ABSOLUTA) ----
  - script: |
      set -e
      EXPORT_PLIST="$(Build.SourcesDirectory)/ios/exportOptions.plist"

      cat > "$EXPORT_PLIST" <<EOF
      <?xml version="1.0" encoding="UTF-8"?>
      <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      <plist version="1.0">
      <dict>
        <key>method</key>
        <string>$(IOS_EXPORT_METHOD)</string>

        <key>teamID</key>
        <string>$(IOS_TEAM_ID)</string>

        <key>signingStyle</key>
        <string>manual</string>

        <key>provisioningProfiles</key>
        <dict>
          <key>$(IOS_BUNDLE_ID)</key>
          <string>$(IOS_PROFILE_NAME)</string>
        </dict>
      </dict>
      </plist>
      EOF

      echo "Export plist en: $EXPORT_PLIST"
      plutil -p "$EXPORT_PLIST" || cat "$EXPORT_PLIST"
      echo "##vso[task.setvariable variable=EXPORT_OPTIONS_PLIST]$EXPORT_PLIST"
    displayName: 'Crear exportOptions.plist (absoluto)'

  # ---- ARCHIVE (forzando Apple Distribution) ----
  - script: |
      set -euo pipefail

      ARCHIVE_PATH="$(Build.ArtifactStagingDirectory)/$(IOS_SCHEME).xcarchive"
      LOG_ARCHIVE="$(Build.ArtifactStagingDirectory)/xcodebuild-archive.log"

      echo "Archiving..."
      echo "Workspace: $(IOS_WORKSPACE)"
      echo "Scheme: $(IOS_SCHEME)"
      echo "Bundle: $(IOS_BUNDLE_ID)"
      echo "Team: $(IOS_TEAM_ID)"
      echo "Profile: $(IOS_PROFILE_NAME)"
      echo "ArchivePath: $ARCHIVE_PATH"

      xcodebuild \
        -workspace "$(IOS_WORKSPACE)" \
        -scheme "$(IOS_SCHEME)" \
        -configuration "$(IOS_CONFIGURATION)" \
        -sdk iphoneos \
        -destination "generic/platform=iOS" \
        -archivePath "$ARCHIVE_PATH" \
        CODE_SIGN_STYLE=Manual \
        DEVELOPMENT_TEAM="$(IOS_TEAM_ID)" \
        PRODUCT_BUNDLE_IDENTIFIER="$(IOS_BUNDLE_ID)" \
        PROVISIONING_PROFILE_SPECIFIER="$(IOS_PROFILE_NAME)" \
        CODE_SIGN_IDENTITY="Apple Distribution" \
        clean archive | tee "$LOG_ARCHIVE"
    displayName: 'Archive iOS (NATIVO)'

  # ---- EXPORT IPA ----
  - script: |
      set -euo pipefail

      ARCHIVE_PATH="$(Build.ArtifactStagingDirectory)/$(IOS_SCHEME).xcarchive"
      EXPORT_PATH="$(Build.ArtifactStagingDirectory)/export"
      LOG_EXPORT="$(Build.ArtifactStagingDirectory)/xcodebuild-export.log"

      mkdir -p "$EXPORT_PATH"

      echo "ExportOptionsPlist: $(EXPORT_OPTIONS_PLIST)"
      ls -la "$(EXPORT_OPTIONS_PLIST)" || true

      xcodebuild \
        -exportArchive \
        -archivePath "$ARCHIVE_PATH" \
        -exportPath "$EXPORT_PATH" \
        -exportOptionsPlist "$(EXPORT_OPTIONS_PLIST)" | tee "$LOG_EXPORT"

      echo "IPA generado:"
      find "$EXPORT_PATH" -maxdepth 2 -name "*.ipa" -print
    displayName: 'Export IPA'

  - task: PublishBuildArtifacts@1
    displayName: 'Publicar artefacto iOS'
    inputs:
      pathToPublish: '$(Build.ArtifactStagingDirectory)/export'
      artifactName: 'ios-ipa'
      publishLocation: 'Container'

  - task: PublishBuildArtifacts@1
    displayName: 'Publicar logs xcodebuild'
    condition: succeededOrFailed()
    inputs:
      pathToPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'ios-build-logs'
      publishLocation: 'Container'
